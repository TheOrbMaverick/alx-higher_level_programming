-- Script to create the database hbtn_0c_0 in MySQL server
-- The script will ignore errors if the database already exists

-- Check if the database hbtn_0c_0 exists, if not, create it
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
